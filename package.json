{
  "name": "stripe-graphql",
  "version": "0.2.0",
  "description": "Stripe API implementation in GraphQL.",
  "main": "src/index.ts",
  "repository": "https://github.com/jferrettiboke/stripe-graphql.git",
  "author": "Jesús Ferretti <jferrettiboke@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "ts-node --log-error src/index.ts",
    "debug": "ts-node-dev --inspect-brk --no-notify --transpileOnly --respawn ./src",
    "dev": "ts-node-dev --no-notify --transpileOnly --respawn -r dotenv/config ./src/server.ts",
    "start": "ts-node --transpile-only src/index.ts"
  },
  "dependencies": {
    "@nexus/schema": "^0.13.1",
    "glob": "^7.1.6",
    "graphql": "^15.0.0",
    "stripe": "^8.39.1"
  },
  "devDependencies": {
    "@types/faker": "^4.1.11",
    "@types/glob": "^7.1.1",
    "@types/stripe": "^7.13.22",
    "apollo-server": "^2.12.0",
    "dotenv": "^8.2.0",
    "faker": "^4.1.0",
    "ts-node-dev": "^1.0.0-pre.32",
    "typescript": "^3.8.3"
  }
}
